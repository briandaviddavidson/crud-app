extends ../layout

block content
  form(action="/lead/" + lead.id + "/edit", method="POST")
    input(name="leadEmail" type="email", placeholder="Update the e-mail" value=lead.email)
    button(type="submit") Save
