doctype html
html(lang='en')
  include templates/head.pug
  +head("CRUD Sales Application")
  body
    h1 Lead
    p 
      span #{lead.email}
      | 
      a(href="/lead/" + lead.id + "/edit") Edit lead
      |
      form(action="/lead/"+lead.id+"/delete", method="POST")
        button(type="submit") Delete
    include templates/footer.pug
    +footer()
    
